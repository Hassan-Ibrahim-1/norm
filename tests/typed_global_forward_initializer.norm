// test: "globals: typed forward initializer should not read an uninitialized later global"
// expect error:
// A 'print' statement is not allowed in global scope

y: int = z + 1;
z: int = 5;

print(y);
print(z);
